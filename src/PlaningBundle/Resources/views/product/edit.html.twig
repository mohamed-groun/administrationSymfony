{% extends 'base.html.twig' %}

{% block body %}
    
 <div class="col-md-3"></div>
 <div class="col-md-4">
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}


        <input type="submit" class="btn btn-success" value="Edit"  />
        <a href="{{ path('product_index') }}"><input type="button"  class="btn btn-primary" value="Back to the list" style="    cursor: pointer;"/></a>
    {{ form_end(edit_form) }}
</div>
</div>


{% endblock %}


     {% extends 'base.html.twig' %}
     {% block body %}

{% if (app.user.username == "sami") or (app.user.username == "asma") or (app.user.username == "abdallah") or (app.user.username == "Mouna") or (app.user.username == "med") %}

    <link href="dist/css/pages/stylish-tooltip.css" rel="stylesheet">

   {% for flashMessage in app.session.flashbag.get('message') %}

             <div class="alert alert-success col-lg-12" role="alert">
                 {{ flashMessage }}
             </div>

         {% endfor %}


      <div class="col-lg-8">
             <div class="card">
                 <div class="card-body">
                     <h4 class="card-title">Mise à jour soldes des congés et les heures d'autorisations<a class="get-code" data-toggle="collapse"
                                                                            href="#tt10" aria-expanded="true"><i
                                     class="fa fa-code" title="Get Code" data-toggle="tooltip"></i></a></h4>
                     <div class="collapse m-t-15" id="tt10" aria-expanded="true"><code>&lt;a class="mytooltip"
                             href="javascript:void(0)"&gt;
                             &lt;i class="fa fa-fw fa-car"&gt;&lt;/i&gt;Car
                             &lt;span class="tooltip-content3">You can easily navigate the city by car.&lt;/span&gt;
                             &lt;/a&gt;
                         </code></div>
                     <p>Pour faire la mise à jour des soldes congé et les heures d'autorisations appuient sur ce bouton  <a class="mytooltip"
                                                                                              href="javascript:void(0)"
                                                                                              style="margin-left: 85px;">
                             <form method="post" action="{{ path('requette') }}">
                                 <input type="submit" class="btn btn-danger" name="validertaux">
                                 <input type="hidden" value="{{ "now"|date("d/m/Y") }}" name="date"></form>
                             <span class="tooltip-content3">
            ATTENTION !!! <br>Cette opération c'est fait une seule fois par mois.</span></a>
                         <span class="under"> Dernier mise a jour  : {{ requette[0]['req'] }}</span>
                     </p>
                 </div>
             </div>
         </div>
<style>
form{
    position: relative;
    top: -42px;
    left: 60%;
}

.under{
    margin-left: 20px;
    text-decoration: underline;
    font-size: 18px;
    text-decoration-color: blue;
    text-decoration-style: double;
}
</style>

  {% else %}
                <div class="alert alert-danger" role="alert">

                    <div class="timeline-panel">
                    <div class="d-flex align-items-center">
                        <i class="ti-lock display-6 m-r-15 text-success"></i>
                        <div>
                            <div class="timeline-heading">
                                <h4 class="timeline-title text-success">Page verrouiller</h4>
                            </div>
                            <div class="timeline-body">
                                <h4>vous n'avez pas l'autorisation d'accéder à cet emplacement !!</h4>
                            </div>
                        </div>
                    </div>
                    <div class="text-right m-t-15">

                    </div>
                    </div>
                 </div>


{% endif %}
{% endblock %}
{% extends 'base.html.twig' %}

{% block body %}
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.0/jquery.min.js"></script>
    <link href="    https://cdnjs.cloudflare.com/ajax/libs/bootstrap-tagsinput/0.8.0/bootstrap-tagsinput.css" rel="stylesheet" />
 <div class="col-md-3"></div>
 <div class="col-md-4">
{% form_theme edit_form 'bootstrap_4_layout.html.twig' %}

    {{ form_start(edit_form) }}
        {{ form_row(edit_form.nomc) }}
     {{ form_row(edit_form.type , {'attr': {'data-role': 'tagsinput'}}) }}

     {{ form_row(edit_form.url) }}
        <div class="row">   
        <input type="submit" class="btn btn-success"  value="Edit" />
        {{ form_end(edit_form) }}


         <a href="{{ path('clients_index') }}"><input type="button"  class="btn btn-primary" value="Back to the list" style="    margin: 0px 16px 0px 15px;    cursor: pointer;"/></a>
       
           
    </div>
     </div>
    {% form_theme edit_form 'bootstrap_4_horizontal_layout.html.twig' %}
    </div>
    <style>
        label.required {
            width: 100%;
        }
    </style>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-tagsinput/0.8.0/bootstrap-tagsinput.min.js"></script>
{% endblock %}

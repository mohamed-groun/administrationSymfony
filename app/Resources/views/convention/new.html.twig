{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <style>
        textarea {
            height: 91px!important;
        }
        ul {
            list-style-type:none;
            position: relative;
            top: -49px;
            left: 45px;

        }

    </style>
{% endblock %}
{% block body %}
    <br>
    <div class="col-4"></div>
    <div class="col-4">
        {{ form_start(form) }}
        {{ form_row(form.photo2) }}
        {{ form_row(form.titre) }}
        {{ form_row(form.description) }}
        <input type="submit" value="Create" class="btn btn-success" />
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('convention_index') }}">
                    <input type="button" class="btn btn-primary" value="Back to the list" style="cursor: pointer; ">
                </a>
            </li>
        </ul>
    </div>
    <div class="col-4"></div>

{% endblock %}
